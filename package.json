{
  "name": "faster-js-for-node",
  "version": "1.0.0",
  "description": "This is a tool that is used to optimize which babel plugins should be used to transform es6/7/next features according to the runtime node enviroment and the babel config",
  "main": "lib/index.js",
  "scripts": {
    "clean": "gulp clean",
    "compile": "gulp compile",
    "build": "gulp build",
    "testCase": "node lib/demoEntry.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && npm run testCase"
  },
  "keywords": [
    "es6",
    "es next",
    "node",
    "babel"
  ],
  "author": "johvin",
  "license": "MIT",
  "engines": {
    "node": ">=6.5.x <7"
  },
  "dependencies": {
    "babel-polyfill": "^6.13.0",
    "bluebird": "^3.4.6",
    "chalk": "^1.1.3",
    "semver": "^5.3.0"
  },
  "devDependencies": {
    "babel-plugin-transform-runtime": "^6.15.0",
    "babel-preset-es2015": "^6.14.0",
    "babel-preset-stage-0": "^6.5.0",
    "gulp": "^3.9.1",
    "gulp-babel": "^6.1.2",
    "gulp-sequence": "^0.4.6",
    "through2": "^2.0.1"
  }
}
